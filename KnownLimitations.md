# Known Limitations

- This environment cannot perform real Windows 7/8/Excel matrix validation. Use `scripts/vm_acceptance_matrix.ps1` on each VM.
- Full offline installers require pre-downloaded wheelhouse and Inno Setup on the build machine.
- Microsoft Excel is never bundled.
